h1 Search recipients (case sensitive exact match)

- if flash[:error]
  .error = flash[:error]

= form_tag recipients_path, method: :get do
  = text_field_tag :name, '', placeholder: 'Name'
  = submit_tag 'Search'

= link_to 'Create a recipient', new_recipient_path
